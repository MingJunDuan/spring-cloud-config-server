scenario NewSpringCloudConfigServer should create a new project based on this archive

let project_name = "new-config-server"
given
  ArchiveRoot

when
  NewSpringCloudConfigServer

then
  fileExists "README.md"
